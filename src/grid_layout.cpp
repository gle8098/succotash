#include "layout.hpp"
#include <cassert>

/*
void GridLayout::Place(const View* parent) {
  const std::vector<View*> views = parent->GetSons();
  const Layout* layout = parent->GetLayout();
  assert(layout->w_count_ != );

  if (layout->w_count_ = 0) {

  }
}
*/
